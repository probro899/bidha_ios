import React from 'react';
import { View, Text } from 'native-base';
import OldDrawer from '../../../screens/drawers';
 
class Settings extends React.Component {
  state={};

  render() {
    return (
      <OldDrawer />
    );
  }
}
export default Settings;
